<?xml version="1.0" encoding="UTF-8"?>
<root>
    <table>
		<sql>
          <![CDATA[
				create table cs_page(
					id bigint not null PRIMARY KEY,					
					page_id bigint default 0,
					parent_id bigint default 0,
					page_ename varchar(250) default '',
					page_cname varchar(250) default '',
					template_id bigint default 0,					
					page_path varchar(250) default '',
					page_interval bigint default 0,
					last_dtime varchar(20) default '',
					next_dtime varchar(20) default '',
					sort_id int default 0,
					page_memo varchar(1000) default '',
					app_id varchar(250) default '',
					site_id varchar(250) default ''
				)
		   ]]> 
       </sql>	   
    </table>	
</root>
 